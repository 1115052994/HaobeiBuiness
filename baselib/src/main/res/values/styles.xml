<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 圆角加载框样式 -->
    <style name="RoundDialogStyle" parent="Theme.AppCompat.Dialog.MinWidth">
        <item name="android:windowBackground">@android:color/transparent</item><!--背景透明-->
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">true</item><!--半透明-->
        <item name="android:windowContentOverlay">@null</item><!--内容覆盖 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item><!-- 窗口样式Dialog -->
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->
    </style>

    <!--横向布满文本-->
    <style name="maxWidthTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!--横向布满文本且居中-->
    <style name="maxWidthCenterTextView" parent="maxWidthTextView">
        <item name="android:gravity">center</item>
    </style>
    <style name="transparentDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 不透明的弹出框样式 -->
    <style name="DialogTranslucent" parent="RoundDialogStyle">
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:backgroundDimEnabled">false</item><!--模糊-->
    </style>


    <!-- 只允许输入数字和字母的编辑框 -->
    <style name="EditTextDigitsLetter">
        <item name="android:digits">@string/input_letter_number</item>
    </style>


</resources>